{
  "buildCommand": "npm run build",
  "outputDirectory": "build",
  "rewrites": [
    { "source": "/api/openai/:path*", "destination": "/api/openai/:path*" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
